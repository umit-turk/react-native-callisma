import React from 'react'
import { View, Text } from 'react-native'

export default function movie({id,title,poster_path,backdrop_path,genres,overview,popularity,vote_avarage,vote_count, genre_ids}) {
    return (
        <View>
            {
                id
            }
        </View>
    )
}
